"""An internal package to serve test suites.

By packaging various samples here:

1.imports are easier; test machinery is harder to import correctly in a monorepo
  if it isn't package properly.
2. decent chance samples can be reused/repurposed for different bits of brewing.
"""
